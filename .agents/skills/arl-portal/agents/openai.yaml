interface:
  display_name: "Arl Portal"
  short_description: "Repo-specific patterns and workflows for arl-portal"
  default_prompt: "Use the arl-portal repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true